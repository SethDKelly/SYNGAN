# SYNGAN Claude Code Bridge

@../AGENTS.md

This file is a compatibility bridge only. Shared repository authority lives in root `AGENTS.md` and canonical `docs/` owners.

Canonical reusable workflows live under `../.agents/skills/`. Thin slash-command bridges under `.claude/commands/` route to those files; they do not duplicate workflow semantics.

When native discovery is unavailable, read the selected canonical `SKILL.md` directly. Do not create Claude-specific product semantics, status, security rules, or change authority.
