# update-traceability

Follow the canonical SYNGAN workflow at `../../.agents/skills/update-traceability/SKILL.md` for the current human-selected task.

Root `AGENTS.md` and canonical repository authorities remain controlling. This bridge adds no permission, scope, semantics, package authorization, or automatic follow-on work.
