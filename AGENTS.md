# SYNGAN Agent Instructions

These instructions apply repository-wide to automated coding/documentation agents.

## Current posture

**SYNGAN has completed Jackson concept design for the current product scope. Phase 013 — Post-Concept Representation & Architecture Reconciliation — is ACTIVE; 013-A is next. Implementation remains held.**

Start with:

- `docs/index.md`
- `docs/problem/problem-purpose.md`
- `docs/authority/design-methodology.md`
- `docs/authority/jackson-design-completion-implementation-hold.md`
- `docs/authority/jackson-methodology-completion-matrix.md`
- `docs/authority/phase-009-dependence-composition-consolidation.md`
- `docs/authority/phase-010-concept-mapping-consolidation.md`
- `docs/authority/phase-011-design-quality-misfit-consolidation.md`
- `docs/authority/phase-012-jackson-concept-design-consolidation.md`
- `docs/authority/residual-conceptual-misfit-register.md`
- `docs/phases/013/index.md`
- `docs/phases/013/013-entry-decomposition.md`
- `docs/architecture/index.md`

## Current state

```text
accepted concepts                    11
active synchronizations              13
Phase 008                            COMPLETE
Phase 009                            COMPLETE
Phase 010                            COMPLETE
Phase 011                            COMPLETE
Phase 012                            COMPLETE
A1-H2                                CURRENTLY CLOSED
Jackson concept design               COMPLETE FOR CURRENT PRODUCT SCOPE
Phase 013                            ACTIVE
013-A                                NEXT ELIGIBLE
R1 architecture reconciliation       DOWNSTREAM / IN PROGRESS
implementation readiness             NOT READY
implementation start                 NOT STARTED
implementation next                  NOT YET
```

## Primary rule

> **Completed concept design is upstream authority for architecture. Retained architecture, code, tests or provider models may expose a genuine misfit, but they may not redefine concept semantics merely to preserve historical implementation choices.**

If Phase 013 discovers a real upstream semantic contradiction, reopen the smallest owning design authority explicitly and revalidate the material blast radius.

## Product-form rule

SYNGAN remains a deployable Python/Spark framework package, agnostic across compliant Spark-capable hosting/infrastructure platforms. Package/SDK, notebook and embedded automation are primary. CLI, reports, rich presentation, service exposure and host/operator integrations remain optional/downstream.

## Completed concept-design authority

Current concept-design package includes:

```text
problem / actor / outcome authority
11 accepted concept specifications
Phase 009 inclusion dependence / application family
13 active synchronizations + singular ownership
Phase 010 concept mapping / parity
Phase 011 quality / misfit / residual register
Phase 012 current-state consolidation / completion decision
```

Current conceptual blockers: **none**.

## Durable quality rules

Decision-material disclosure:

> **Progressive disclosure may defer explanatory depth, but it must not defer a qualifier whose omission could change the actor's immediate semantic decision or make current state appear stronger than the owning concept supports.**

Provider-evidence qualification:

> **Consume host/provider facts only at the evidentiary strength they actually establish. Provider words such as `success`, `completed`, `model`, `artifact`, `lineage`, `current`, or `production` do not automatically become stronger SYNGAN semantic claims.**

Future rediscovery:

> **Genericity means accepting new instances within a stable purpose. Rediscover before implementation when future scope introduces an independent product-facing purpose with durable state/history and independently meaningful actions/lifecycle.**

## Phase 013 explicit carry-forward

### M6 reconciliation item

Retained Phase 006/007 representation/architecture documents contain historical synchronization assumptions such as `11 / 15` and older `SYNC-08` / `SYNC-15` roles. Current Phase 009 authority supersedes those semantics.

Treat this as:

```text
MAT-1 / M6
PHASE 013 REPRESENTATION / ARCHITECTURE DOCUMENTATION RECONCILIATION
```

Do not reopen synchronization semantics merely to preserve old numbering.

### M8 future triggers

Formal composable privacy/accounting, product-owned governance/release, independent output publication/versioning/retirement, reusable request/cohort lifecycle, graph/relationship lifecycle, durable streaming/session/feed lifecycle, resource/economic accounting and reusable knowledge/memory remain **future rediscovery triggers**.

Do not create architecture placeholders for them unless future product scope first re-enters concept discovery.

## Phase 013 decomposition

```text
013-A  reconciliation authority / retained corpus inventory / precedence reset / discrepancy taxonomy
013-B  representation layering / public contract / identity / revision / handles / views
013-C  control persistence / historical reference / transactions / concurrency / migration
013-D  distributed data / structured topology / manifest / candidate / seal / promotion
013-E  Strategy/method realization / dependency closure / authorization / secrets / offline-no-egress
013-F  Execution / Attempt / fencing / idempotency / checkpoint / cancellation / recovery / admission
013-G  Evaluation / Evidence / Provenance / historical query / reproducibility / disclosure
013-H  deployment / scalability / observability / portability / compatibility / platform integration
013-I  cross-architecture composition / ADR + legacy reconciliation / M6 cleanup / residual register
013-J  Phase 013 consolidation / R1 decision / Phase 014 handoff
```

The dependency order is strict by default. Do not clean up later architecture/ADR wording before the owning architecture domain has been reconciled.

## 013-A discipline

013-A must establish the reconciliation method before substantive architecture corrections.

At minimum it must:

- inventory retained architecture, ADRs and planning/scaffold evidence;
- reset authority precedence beneath Phase 012 completed concept design;
- classify architecture statements/findings as retain, clarify, supersede, correct, defer or upstream-reopen;
- define materiality and smallest-architecture-authority correction rules;
- distinguish semantic contradiction from representation drift, stale terminology, historical-only material and implementation prescription;
- inventory all current-looking `11 / 15`, `SYNC-08`, `SYNC-15` and similar historical references as candidates;
- define how later 013 groups record affected documents and revalidation obligations;
- establish the residual architecture register shape for 013-I.

Do not perform broad architecture rewrites inside 013-A.

## Architecture reconciliation guardrails

Phase 013 must specifically reject or correct architecture that:

- duplicates/transfers canonical concept ownership;
- lets provider job/run state drive Learning/Generation/Evaluation semantic completion;
- substitutes provider model/artifact/catalog identity for canonical Strategy/Learned State/Data Meaning/result identity;
- treats provider lineage as complete Provenance/source truth;
- allows recovery/restoration to resurrect stale authority;
- treats checkpoint/manifest/material existence as semantic result establishment;
- treats driver-local dependency availability as distributed runtime closure;
- silently weakens committed quantity/topology/Evaluation/security semantics under scale pressure;
- creates global `status`, `workflow`, `validation`, `recovery` or `degraded` authority;
- reserves architecture for M8 concepts before rediscovery.

Representation mechanisms such as handles, references, manifests, Attempts, bindings, capabilities, projections and platform correlations remain architecture roles unless upstream concept authority says otherwise.

## Architecture / implementation boundary

Phase 013 is design/reconciliation only.

Do not begin production concept behavior, migrations, provider adapter implementation, API stabilization, persistence rollout, recovery/fencing implementation, package refactoring, runtime integration, benchmarks or executable conformance work merely because Jackson concept design is complete.

Historical Phase 007-B/007-C scaffold and package/tooling rules are evidence to be reconciled in 013-I; they are not automatically current architecture.

## Readiness rule

```text
Phase 013  representation / architecture reconciliation — ACTIVE
Phase 014  whole-design implementation-readiness decision
Phase 015  implementation authority / controlled delivery — FUTURE ONLY
```

Until Phase 014 positively passes:

```text
IMPLEMENTATION READINESS   NOT READY
IMPLEMENTATION START       NOT STARTED
IMPLEMENTATION NEXT        NOT YET
```

## Current next boundary

**013-A — Reconciliation Authority, Retained Corpus Inventory, Precedence Reset & Discrepancy Taxonomy** is next eligible.
